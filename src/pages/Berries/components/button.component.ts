import { styled } from "@stitches/react";

const Group = styled("div", {
  padding: "1rem 0",

  display: "flex",
  justifyContent: "center",
  gap: "1rem",
});

const Button = {
  Group: Group,
};

export default Button;
