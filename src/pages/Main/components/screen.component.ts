"use client";

import { styled } from "@stitches/react";

const ScreenStyled = styled("main", {
  minHeight: "100vh",
  backgroundColor: "#eab646",
  display: "flex",
  alignItems: "center",
});

export default ScreenStyled;
